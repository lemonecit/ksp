# Railway configuration for KSP
# This file is for the API service
# Web service should be added separately in Railway dashboard

[build]
builder = "nixpacks"
buildCmd = "npm run build:api"

[deploy]
startCmd = "npm run start:api"
restartPolicyType = "ON_FAILURE"
