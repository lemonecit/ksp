{
  "name": "ksp-affiliate-system",
  "version": "1.0.0",
  "description": "KSP Affiliate Revenue System with bilingual support",
  "private": true,
  "scripts": {
    "dev:api": "cd apps/api && npm run dev",
    "dev:web": "cd apps/web && npm run dev",
    "dev:admin": "cd apps/admin && npm run dev",
    "scrape": "cd apps/scraper && npm run scrape",
    "translate": "cd apps/scraper && npm run translate",
    "db:migrate": "cd packages/database && npx prisma migrate dev",
    "db:generate": "cd packages/database && npx prisma generate",
    "db:studio": "cd packages/database && npx prisma studio",
    "install:all": "npm install && cd packages/database && npm install && cd ../../apps/api && npm install && cd ../web && npm install && cd ../admin && npm install && cd ../scraper && npm install"
  },
  "devDependencies": {
    "typescript": "^5.3.0"
  }
}
